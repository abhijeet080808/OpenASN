language: cpp

os: linux
dist: bionic

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-7
      - g++-7
      - cmake

script:
  - cd build
  - cmake ..
  - make
  - ctest
